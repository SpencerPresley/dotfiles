<superpowers_skill_workflow_guidance>
The `superpowers` skills are extremely powerful, and should always act as the default for any applicable workflows.

Always prefer a skill from `superpowers` over any other similar skill.

If in doubt always use the `superpowers:using-superpowers` skill to review usage. It is completely accetable to use this numerous times throughout a conversation.

1. Use the **`superpowers:brainstorming`** skill.
2. After brainstorming is complete use the `superpowers:writing-plans` skill
3. Once the plan has been created and approved use the `superpowers:executing-plans` skill

During an `executing-plans` phase, if the User selects `subagent-driven-development`:

ALWAYS inform any subagents the correct way to use `uv` for:
- Running python
- Running tests
- Using ruff
- Using pyright

Ensure YOU (Claude) understand how to use `uv` for those things so that you can confidently inform the subagents.

When following subagent-driven-development:
- Always have individual agents for each domain
  - Implementing
  - Spec alignment review
  - Code quality review
- NEVER set the model for any subagents directly. Spencer has explicitly set the model to `inherit`, do NOT explicitly set the model thus overwriting Spencer's decision.

Always consider these other complimentary superpower skills:
- `superpowers:test-driven-development`
- `superpowers:using-git-worktrees`
- `superpowers:systematic-debugging`
- `verification-before-completion`

They are worth a read just to be sure.
</superpowers_skill_workflow_guidance>
